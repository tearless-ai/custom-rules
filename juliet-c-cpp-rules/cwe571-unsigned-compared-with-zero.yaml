rules:
- id: cwe571-unsigned-compared-with-zero
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: An unsigned value is compared against zero in a way that is always true, because an unsigned value can never be negative.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      An unsigned value is compared against zero in a way that is always true, because an unsigned value can never be negative.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
    policy_name: 'Expression Always True: Unsigned Compared With Zero'
  pattern-either:
  - pattern: (unsigned int $U) >= 0
  - pattern: (unsigned $U) >= 0
  - pattern: (unsigned long $U) >= 0
  - pattern: (size_t $U) >= 0
  - pattern: 0 <= (unsigned int $U)
