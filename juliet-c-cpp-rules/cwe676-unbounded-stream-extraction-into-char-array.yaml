rules:
- id: cwe676-unbounded-stream-extraction-into-char-array
  languages:
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: A string is extracted from an input stream into a fixed-size character array without setting width() first, which can overflow the buffer.
  metadata:
    vulnerability_class: Use of Potentially Dangerous Function
    help: |
      ## Summary
      A string is extracted from an input stream into a fixed-size character array without setting width() first, which can overflow the buffer.

      ## Remediation
      Replace potentially dangerous functions with bounded, safer alternatives and validate their inputs.
    category: security
    cwe:
    - 'CWE-676: Use of Potentially Dangerous Function'
    references:
    - https://cwe.mitre.org/data/definitions/676.html
    confidence: MEDIUM
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Replace potentially dangerous functions with bounded, safer alternatives and validate their inputs.
    policy_name: 'Dangerous Function: Unbounded Stream Extraction into Array'
  patterns:
  - pattern: $IN >> $BUF
  - pattern-either:
    - pattern-inside: |
        char $BUF[$N];
        ...
    - pattern-inside: |
        wchar_t $BUF[$N];
        ...
  - metavariable-regex:
      metavariable: $IN
      regex: ^(std::)?(cin|wcin)$
  - pattern-not-inside: |
      $IN.width($W);
      ...
  - pattern-not-inside: |
      $IN >> setw($W) >> $BUF
