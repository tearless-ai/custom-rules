rules:
- id: cwe571-constant-condition
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: This condition always evaluates to true because both operands are compile-time constants.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      This condition always evaluates to true because both operands are compile-time constants.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
  patterns:
  - pattern-either:
    - pattern: if (1) $S;
    - pattern: if (1) $S; else $S2;
    - patterns:
      - pattern: if ($A == $B) $S;
      - metavariable-comparison:
          comparison: ($A == $B)
    - patterns:
      - pattern: if ($A != $B) $S;
      - metavariable-comparison:
          comparison: ($A != $B)
    - patterns:
      - pattern: if ($A < $B) $S;
      - metavariable-comparison:
          comparison: ($A < $B)
    - patterns:
      - pattern: if ($A <= $B) $S;
      - metavariable-comparison:
          comparison: ($A <= $B)
    - patterns:
      - pattern: if ($A > $B) $S;
      - metavariable-comparison:
          comparison: ($A > $B)
    - patterns:
      - pattern: if ($A >= $B) $S;
      - metavariable-comparison:
          comparison: ($A >= $B)
