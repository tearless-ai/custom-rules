rules:
- id: cwe571-condition-on-constant-function
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: $F always returns $K, so this condition is always true.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      `$F` always returns `$K`, so this condition is always true.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
  patterns:
  - pattern-either:
    - pattern-inside: |
        static $T $F() { return $K; }
        ...
    - pattern-inside: |
        static $T $F(void) { return $K; }
        ...
  - metavariable-regex:
      metavariable: $K
      regex: ^-?[0-9]+$
  - pattern: if ($F()) $S;
  - metavariable-comparison:
      comparison: $K != 0
  - focus-metavariable: $F
