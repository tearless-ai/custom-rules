rules:
- id: cwe253-pointer-return-compared-negative
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: $F() returns a pointer (NULL on failure), so comparing its result against a negative bound
    never detects the error.
  metadata:
    vulnerability_class: Incorrect Check of Function Return Value
    help: |
      ## Summary
      `$F()` returns a pointer (NULL on failure), so comparing its result against a negative bound never detects the error.

      ## Remediation
      Compare return values against the function's documented error value (e.g. EOF, NULL, INVALID_HANDLE_VALUE, -1) rather than an assumed one.
    category: security
    cwe:
    - 'CWE-253: Incorrect Check of Function Return Value'
    references:
    - https://cwe.mitre.org/data/definitions/253.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Compare return values against the function's documented error value (e.g. EOF, NULL,
      INVALID_HANDLE_VALUE, -1) rather than an assumed one.
  patterns:
  - pattern-either:
    - pattern: $F(...) < 0
    - pattern: $F(...) <= -1
    - pattern: $F(...) >= 0
  - metavariable-regex:
      metavariable: $F
      regex: ^(fgets|fgetws|fopen|_wfopen|malloc|calloc|realloc|strdup|getenv|tmpfile)$
