rules:
- id: cwe570-compare-against-type-limit
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: A value is compared against the minimum or maximum of its own type in a way that is always false.
  metadata:
    vulnerability_class: Expression is Always False
    help: |
      ## Summary
      A value is compared against the minimum or maximum of its own type in a way that is always false.

      ## Remediation
      Fix conditions that are always false; they indicate a logic error or dead code.
    category: correctness
    cwe:
    - 'CWE-570: Expression is Always False'
    references:
    - https://cwe.mitre.org/data/definitions/570.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always false; they indicate a logic error or dead code.
    policy_name: 'Expression Always False: Comparison With Type Limit'
  pattern-either:
  - pattern: (int $X) < INT_MIN
  - pattern: (int $X) > INT_MAX
  - pattern: (long $X) < LONG_MIN
  - pattern: (long $X) > LONG_MAX
