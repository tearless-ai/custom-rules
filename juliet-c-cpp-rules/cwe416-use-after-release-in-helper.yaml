rules:
- id: cwe416-use-after-release-in-helper
  languages:
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: ERROR
  message: A pointer is used after a helper function in this file released the memory it points to through a reference parameter.
  metadata:
    vulnerability_class: Use After Free
    help: |
      ## Summary
      A pointer is used after a helper function in this file released the memory it points to through a reference parameter.

      ## Remediation
      Do not use memory after it has been freed; set pointers to NULL after free and restructure ownership so no stale references remain.
    category: security
    cwe:
    - 'CWE-416: Use After Free'
    references:
    - https://cwe.mitre.org/data/definitions/416.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: HIGH
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Do not use memory after it has been freed; set pointers to NULL after free and restructure ownership so no stale references remain.
    policy_name: 'Use After Free: Pointer Used After Helper Released It'
  pattern-either:
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... free($D); ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $UF(..., $Y, ...)
    - focus-metavariable: $Y
    - metavariable-regex:
        metavariable: $UF
        regex: ^(str|wcs|mem|print|puts|fputs|putws|fputws|fwrite|fread|fgets|fgetws|scanf|sscanf|fscanf|swscanf|atoi|atol|atof|sprintf|snprintf|swprintf|fprintf|vsprintf|vfprintf)\w*$
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... free($D); ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y[$UI]
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... free($D); ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: '*$Y'
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... free($D); ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y->$UFLD
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $UF(..., $Y, ...)
    - focus-metavariable: $Y
    - metavariable-regex:
        metavariable: $UF
        regex: ^(str|wcs|mem|print|puts|fputs|putws|fputws|fwrite|fread|fgets|fgetws|scanf|sscanf|fscanf|swscanf|atoi|atol|atof|sprintf|snprintf|swprintf|fprintf|vsprintf|vfprintf)\w*$
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y[$UI]
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: '*$Y'
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y->$UFLD
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete [] $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $UF(..., $Y, ...)
    - focus-metavariable: $Y
    - metavariable-regex:
        metavariable: $UF
        regex: ^(str|wcs|mem|print|puts|fputs|putws|fputws|fwrite|fread|fgets|fgetws|scanf|sscanf|fscanf|swscanf|atoi|atol|atof|sprintf|snprintf|swprintf|fprintf|vsprintf|vfprintf)\w*$
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete [] $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y[$UI]
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete [] $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: '*$Y'
    - focus-metavariable: $Y
  - patterns:
    - pattern-inside: |
        $R1 $S($PT & $D) { ... delete [] $D; ... }
        ...
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $R2 $G(...) { ... $S($Y); ... }
    - pattern-inside: |
        $S($Y);
        ...
    - pattern: $Y->$UFLD
    - focus-metavariable: $Y
