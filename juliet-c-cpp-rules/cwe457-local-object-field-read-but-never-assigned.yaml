rules:
- id: cwe457-local-object-field-read-but-never-assigned
  languages:
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: ERROR
  message: A field of a local object that has no initializer is read, but neither the object nor the field is ever assigned in the function, so the field value is indeterminate unless the type has a constructor that sets it.
  metadata:
    vulnerability_class: Use of Uninitialized Variable
    help: |
      ## Summary
      A field of a local object that has no initializer is read, but neither the object nor the field is ever assigned in the function, so the field value is indeterminate unless the type has a constructor that sets it.

      ## Remediation
      Initialize every variable before it is read.
    category: security
    cwe:
    - 'CWE-457: Use of Uninitialized Variable'
    references:
    - https://cwe.mitre.org/data/definitions/457.html
    confidence: MEDIUM
    likelihood: MEDIUM
    impact: HIGH
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Initialize every variable before it is read.
    policy_name: 'Uninitialized Variable: Field of Unassigned Local Object (C++)'
  patterns:
  - pattern-inside: |
      $R $FN(...) { ... $T $X; ... }
  - metavariable-regex:
      metavariable: $T
      regex: ^(?!(std::|const\b)).*[^*]$
  - pattern-not-inside: |
      $R $FN(...) { ... $T2 $X[$N]; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... $X = $V ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... $X.$FLD = $V ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... $X[$I] = $V ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... &$X ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... &$X.$FLD ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... $STREAM >> $X ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... va_start($X, ...); ... }
  - pattern-not-inside: |
      $R $FN(...) { ... va_copy($X, ...); ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... memcpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... memmove($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... memset($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... wmemset($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... wmemcpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... strcpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... wcscpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... strncpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... wcsncpy($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... sprintf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... snprintf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... swprintf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... fgets($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... fgetws($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... fread($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... read($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... recv($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... scanf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... sscanf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... fscanf($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R $FN(...) { ... <... gets($X.$FLD, ...) ...>; ... }
  - pattern-not-inside: |
      $R2 $SRC(..., $PT &$P, ...) { ... $P = $V; ... }
      ...
      $R $FN(...) { ... $SRC(..., $X, ...); ... }
  - pattern-not-inside: |
      $R2 $SRC(..., $PT &$P, ...) { ... $P.$PF = $V; ... }
      ...
      $R $FN(...) { ... $SRC(..., $X, ...); ... }
  - pattern-not-inside: |
      $R2 $SRC(..., $PT &$P, ...);
      ...
      $R $FN(...) { ... $SRC(..., $X, ...); ... }
  - pattern: $G(..., $X.$FLD, ...)
  - pattern-not: sizeof($X)
  - pattern-not-inside: |
      $R2 $G(..., $PT &$P, ...) { ... }
      ...
  - pattern-not-inside: |
      $R2 $G(..., $PT &$P, ...);
      ...
