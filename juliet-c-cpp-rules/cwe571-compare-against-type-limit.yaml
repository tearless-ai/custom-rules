rules:
- id: cwe571-compare-against-type-limit
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: A value is compared against the minimum or maximum of its own type in a way that is always true.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      A value is compared against the minimum or maximum of its own type in a way that is always true.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
    policy_name: 'Expression Always True: Comparison With Type Limit'
  pattern-either:
  - pattern: (int $X) <= INT_MAX
  - pattern: (int $X) >= INT_MIN
  - pattern: (long $X) <= LONG_MAX
  - pattern: (long $X) >= LONG_MIN
