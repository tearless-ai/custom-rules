rules:
- id: cwe121-struct-size-copied-into-member-of-stack-struct
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: ERROR
  message: A copy into one member array of a stack structure uses the size of the whole structure, overrunning the member.
  metadata:
    vulnerability_class: Stack-based Buffer Overflow
    help: |
      ## Summary
      A copy into one member array of a stack structure uses the size of the whole structure, overrunning the member.

      ## Remediation
      Bound every copy and index by the destination's size (sizeof the array), use size-checked APIs such as snprintf, and validate lengths and indexes from input before use.
    category: security
    cwe:
    - 'CWE-121: Stack-based Buffer Overflow'
    references:
    - https://cwe.mitre.org/data/definitions/121.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: HIGH
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Bound every copy and index by the destination's size (sizeof the array), use size-checked APIs such as snprintf, and validate lengths and indexes from input before use.
    policy_name: 'Stack Buffer Overflow: Whole Struct Size Copied Into Member'
  patterns:
  - pattern-either:
    - pattern: memcpy($S.$FLD, $X, sizeof($S))
    - pattern: memmove($S.$FLD, $X, sizeof($S))
  - pattern-inside: |-
      $RET $F(...) {
        ...
        $TY $S;
        ...
      }
