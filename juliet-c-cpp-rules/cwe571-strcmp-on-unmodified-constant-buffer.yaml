rules:
- id: cwe571-strcmp-on-unmodified-constant-buffer
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: A character buffer initialized from a string literal and never modified is compared with another literal, so the result is always true.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      A character buffer initialized from a string literal and never modified is compared with another literal, so the result is always true.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
    policy_name: 'Expression Always True: Comparison of Unmodified Constant String'
  patterns:
  - pattern-either:
    - pattern-inside: |
        char $S[$N] = $A;
        ...
    - pattern-inside: |
        wchar_t $S[$N] = $A;
        ...
  - pattern-not-inside: |
      $F(..., $S, ...);
      ...
  - pattern-either:
    - pattern: strcmp($S, $B) == 0
    - pattern: wcscmp($S, $B) == 0
  - metavariable-regex:
      metavariable: $B
      regex: ^L?".*"$
  - metavariable-comparison:
      comparison: str($A) == str($B)
