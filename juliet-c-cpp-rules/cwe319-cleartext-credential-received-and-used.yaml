rules:
- id: cwe319-cleartext-credential-received-and-used
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: ERROR
  message: A credential is received from the network with $RECV() and used for authentication in the same
    function without being decrypted, so it travels in cleartext.
  metadata:
    vulnerability_class: Cleartext Transmission of Sensitive Information
    help: |
      ## Summary
      A credential is received from the network with `$RECV()` and used for authentication in the same function without being decrypted, so it travels in cleartext.

      ## Remediation
      Encrypt sensitive data in transit with TLS instead of sending it over plain sockets.
    category: security
    cwe:
    - 'CWE-319: Cleartext Transmission of Sensitive Information'
    references:
    - https://cwe.mitre.org/data/definitions/319.html
    confidence: MEDIUM
    likelihood: MEDIUM
    impact: HIGH
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Encrypt sensitive data in transit with TLS instead of sending it over plain sockets.
  patterns:
  - pattern: $RECV($SOCK, $BUF, ...)
  - metavariable-regex:
      metavariable: $RECV
      regex: ^(recv|recvfrom|recvmsg|WSARecv|WSARecvFrom|read|SSL_read_early_data)$
  - pattern-inside: |
      $T_F $F(...) {
        ...
        <... $RECV(...) ...>;
        ...
        <... $LOGON(...) ...>;
        ...
      }
  - metavariable-regex:
      metavariable: $LOGON
      regex: ^(LogonUser|LogonUserA|LogonUserW|LogonUserExA|LogonUserExW|pam_authenticate|crypt|crypt_r|CredUIPromptForCredentialsA|CredUIPromptForCredentialsW)$
  - pattern-not-inside: |
      $T_F $F(...) {
        ...
        <... CryptDecrypt(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_F $F(...) {
        ...
        <... BCryptDecrypt(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_F $F(...) {
        ...
        <... CryptUnprotectData(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_F $F(...) {
        ...
        <... EVP_DecryptUpdate(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_F $F(...) {
        ...
        <... EVP_OpenUpdate(...) ...>;
        ...
      }
  - focus-metavariable: $RECV
