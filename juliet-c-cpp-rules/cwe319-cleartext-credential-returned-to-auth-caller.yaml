rules:
- id: cwe319-cleartext-credential-returned-to-auth-caller
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: ERROR
  message: A credential received from the network with $RECV() is returned to a caller that uses it for authentication without decrypting it, so it travels in cleartext.
  metadata:
    vulnerability_class: Cleartext Transmission of Sensitive Information
    help: |
      ## Summary
      A credential received from the network with `$RECV()` is returned to a caller that uses it for authentication without decrypting it, so it travels in cleartext.

      ## Remediation
      Encrypt sensitive data in transit with TLS instead of sending it over plain sockets.
    category: security
    cwe:
    - 'CWE-319: Cleartext Transmission of Sensitive Information'
    references:
    - https://cwe.mitre.org/data/definitions/319.html
    confidence: MEDIUM
    likelihood: MEDIUM
    impact: HIGH
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Encrypt sensitive data in transit with TLS instead of sending it over plain sockets.
  patterns:
  - pattern: $RECV($SOCK, $BUF, ...)
  - metavariable-regex:
      metavariable: $RECV
      regex: ^(recv|recvfrom|recvmsg|WSARecv|WSARecvFrom|read)$
  - pattern-inside: |
      $T_S $SRCFN(...) { ... }
  - pattern-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F $F(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... $LOGON(...) ...>;
        ...
      }
  - metavariable-regex:
      metavariable: $LOGON
      regex: ^(LogonUser|LogonUserA|LogonUserW|LogonUserExA|LogonUserExW|pam_authenticate|crypt|crypt_r)$
  - pattern-not-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F2 $F2(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... CryptDecrypt(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F2 $F2(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... BCryptDecrypt(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F2 $F2(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... CryptUnprotectData(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F2 $F2(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... EVP_DecryptUpdate(...) ...>;
        ...
      }
  - pattern-not-inside: |
      $T_S $SRCFN(...) { ... }
      ...
      $T_F2 $F2(...) {
        ...
        <... $SRCFN(...) ...>;
        ...
        <... EVP_OpenUpdate(...) ...>;
        ...
      }
  - focus-metavariable: $RECV
