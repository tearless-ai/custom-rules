rules:
- id: cwe571-self-comparison-with-offset
  languages:
  - c
  - cpp
  paths:
    include:
    - '*.c'
    - '*.h'
    - '*.cpp'
    - '*.cc'
    - '*.cxx'
    - '*.c++'
    - '*.hpp'
    - '*.hh'
    - '*.hxx'
    - '*.inc'
  severity: WARNING
  message: A variable is compared for equality with itself plus or minus a non-zero constant, which is always true.
  metadata:
    vulnerability_class: Expression is Always True
    help: |
      ## Summary
      A variable is compared for equality with itself plus or minus a non-zero constant, which is always true.

      ## Remediation
      Fix conditions that are always true; they indicate a logic error or a missing check.
    category: correctness
    cwe:
    - 'CWE-571: Expression is Always True'
    references:
    - https://cwe.mitre.org/data/definitions/571.html
    confidence: HIGH
    likelihood: MEDIUM
    impact: MEDIUM
    subcategory:
    - vuln
    technology:
    - c
    - cpp
    source: Validated against NIST Juliet C/C++ Test Suite v1.3
    remediation: Fix conditions that are always true; they indicate a logic error or a missing check.
    policy_name: 'Expression Always True: Variable Compared With Itself Offset'
  patterns:
  - pattern-either:
    - pattern: $X != $X + $K
    - pattern: $X != ($X + $K)
    - pattern: $X != $X - $K
    - pattern: $X != ($X - $K)
  - metavariable-regex:
      metavariable: $K
      regex: ^[1-9][0-9]*[uUlL]*$
